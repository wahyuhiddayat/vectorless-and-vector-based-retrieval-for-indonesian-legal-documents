\documentclass[tikz,border=14pt]{standalone}
\usepackage[T1]{fontenc}
\usepackage{helvet}
\renewcommand{\familydefault}{\sfdefault}
\usepackage{amsmath}
\usetikzlibrary{arrows.meta, fit, backgrounds, positioning, calc}

\definecolor{queryfill}{HTML}{DFF7F3}
\definecolor{queryborder}{HTML}{1BA99C}
\definecolor{corpusfill}{HTML}{FFF4D6}
\definecolor{corpusborder}{HTML}{D9A441}
\definecolor{bm25fill}{HTML}{FFE9C8}
\definecolor{bm25border}{HTML}{D88922}
\definecolor{outfill}{HTML}{E7F6E7}
\definecolor{outborder}{HTML}{4FA64F}
\definecolor{poolfill}{HTML}{F1F9F1}
\definecolor{regfill}{HTML}{F6F8FB}
\definecolor{regborder}{HTML}{C4CDD6}
\definecolor{darkline}{HTML}{333333}
\definecolor{subtext}{HTML}{5A4632}

\tikzset{
  card/.style={rectangle, rounded corners=5pt, align=center, draw=darkline,
               line width=0.8pt, minimum height=1.2cm, font=\small},
  query/.style={card, fill=queryfill, draw=queryborder, minimum width=2.3cm},
  proc/.style={card, fill=bm25fill, draw=bm25border, minimum width=3.4cm,
               minimum height=1.25cm},
  result/.style={card, fill=outfill, draw=outborder, minimum width=2.6cm,
                 minimum height=1.25cm},
  docbox/.style={rectangle, rounded corners=4pt, align=center,
                 draw=corpusborder, fill=corpusfill, line width=0.8pt,
                 minimum width=2.4cm, minimum height=1.1cm, font=\small},
  leaf/.style={rectangle, rounded corners=3pt, draw=outborder, fill=outfill,
               line width=0.8pt, minimum width=1.15cm, minimum height=0.95cm, font=\scriptsize},
  region/.style={rounded corners=12pt, fill=regfill, draw=regborder, line width=0.8pt},
  pooldash/.style={draw=outborder, dashed, rounded corners=8pt, fill=poolfill,
                   line width=0.85pt},
  regtitle/.style={font=\small\bfseries, text=darkline},
  note/.style={font=\scriptsize\itshape, text=subtext, align=center},
  flow/.style={-{Latex[length=2.5mm]}, line width=0.95pt, draw=darkline},
  branch/.style={-{Latex[length=2.0mm]}, line width=0.75pt, draw=darkline},
}

%% Same spacing knobs as bm25-tree for cross-figure consistency.
\def\intra{1.5cm}
\def\inter{2.4cm}

\begin{document}
\begin{tikzpicture}[node distance=\inter]

%% ── Corpus flattening ───────────────────────────────────────────────────────
\node[query] (query) {Query $q$};
\node[docbox, right=\inter of query] (corpus) {Hierarchical\\Corpus};

%% all leaf nodes (flattened) as a dashed pool; leaves drawn, pool, redraw leaves
\node[leaf, right=\inter of corpus, yshift=1.55cm] (lf1) {$l_1$};
\node[leaf, right=\inter of corpus] (lf2) {$l_2$};
\node[leaf, right=\inter of corpus, yshift=-1.55cm] (lf3) {$l_3$};
\node[pooldash, fit=(lf1)(lf2)(lf3), inner sep=7pt] (pool) {};
\foreach \n/\t in {lf1/l_1, lf2/l_2, lf3/l_3} {
  \node[leaf] at (\n) {$\t$};
}

%% ── BM25 scoring ────────────────────────────────────────────────────────────
\node[proc, right=\inter of pool] (enrich) {Leaf Enrichment};
\node[proc, right=\intra of enrich] (score) {BM25 Scoring};
\node[result, right=\inter of score] (sources) {Final Ranked\\Sources};

%% ── Notes (italic, outside) ─────────────────────────────────────────────────
\node[note, below=0.5cm of corpus] (corpusnote) {All documents\\$D_i \in \mathcal{D}$};
\node[note, below=0.4cm of pool] (poolnote) {Flattened leaf nodes\\(All leaves across corpus)};
\node[note, below=0.5cm of enrich] (enrichnote) {Enrich with parent title, path,\\text, and penjelasan};
\node[note, below=0.5cm of score] (scorenote) {Score enriched leaf texts\\and select top-$K$};

%% ── Regions ─────────────────────────────────────────────────────────────────
\begin{scope}[on background layer]
  \node[region, fit=(query)(corpus)(corpusnote)(pool)(poolnote),
        inner xsep=18pt, inner ysep=18pt] (reg1) {};
  \node[region, fit=(enrich)(enrichnote)(score)(scorenote)(sources),
        inner xsep=18pt, inner ysep=18pt] (reg2) {};
\end{scope}
\node[regtitle, anchor=south] at ($(reg1.north)+(0,0.18)$) {Stage 1: Corpus Flattening};
\node[regtitle, anchor=south] at ($(reg2.north)+(0,0.18)$) {Stage 2: BM25 Scoring \& Ranking};

%% ── Flow ────────────────────────────────────────────────────────────────────
\draw[flow] (query)  -- (corpus);
\draw[flow] (corpus.east) -- (pool.west);
\draw[flow] (pool.east) -- (enrich.west);
\draw[flow] (enrich) -- (score);
\draw[flow] (score.east) -- (sources.west);

\end{tikzpicture}
\end{document}